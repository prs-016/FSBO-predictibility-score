[project]
name = "fsbo-predictability-score"
version = "0.1.0"
description = "Live next-attack prediction for volleyball, fed by a DataVolley scout file."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
